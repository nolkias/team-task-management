server:
  port: 8080
  shutdown_timeout_seconds: 10

database:
  max_open_conns: 25
  max_idle_conns: 25
  conn_max_lifetime_minutes: 5

pagination:
  default_page_size: 20
  max_page_size: 100

cache:
  task_list_ttl_seconds: 300

rate_limit:
  requests_per_minute: 100

jwt:
  expiry_hours: 24
